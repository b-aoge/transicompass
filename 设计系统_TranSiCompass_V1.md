# 转型罗盘 TranSiCompass — 设计系统 V1

设计：颜好看（UI/UX） | 日期：2026-08-03 | 状态：定稿待评审
配套文件：`design-tokens.json`（机器可读）、`tokens.css`（可直接引入）

---

## 0. 设计判断前提

| 维度 | 判定 | 依据 |
|------|------|------|
| 设计寄存器 | **Product（工具型）** | 核心是诊断结果页，用户从小红书带着焦虑跳转进来只为拿结论。落地页是输入页不是营销长页。故走克制策略：中性色 70-90% + 单一强调色 ≤10% |
| 平台轴 | **Web，移动优先** | 微信内置浏览器（iOS Safari 内核 / 安卓 X5 内核）是主要落地环境 |
| DESIGN_VARIANCE | **4 / 10** | 工程人要「稳」不要艺术混沌。左对齐为主、非居中 Hero、分屏产生非对称节奏，但网格可预测 |
| MOTION_INTENSITY | **3 / 10** | 焦虑用户 + 对花哨不信任。只保留功能性动效：状态确认、展开折叠、进度反馈。零装饰动画 |
| VISUAL_DENSITY | **6 / 10** | 结果页信息量大，工程人日常读图纸和技术文档，适度密集反而增加可信度。移动端降至 5 |

### 品牌声音三词（物理对象锚定，非形容词）

> **精确 · 沉稳 · 定向**
>
> 物理参照：**施工图纸的蓝晒图**（深钢蓝底、白线、标题栏）、**水准仪的刻度盘**（读数、刻度、等宽数字）、**岩层剖面**（分层、递进、有重量）。

这三个锚定物直接决定了后面所有决策：主色是蓝晒靛而非任何一种流行蓝；匹配度用**刻度条 + 等宽数字**而非环形饼图；层级用**表面明度递进**而非彩色边框。

### 一句话设计目标

**让一个晚上 10 点在项目部宿舍刷手机的 35 岁施工员，在 5 秒内确认"这东西是给我做的、不是来忽悠我的"。**

---

## 1. Visual Theme & Atmosphere

### 视觉主题关键词

`蓝晒图纸` · `仪器读数` · `技术文档` · `克制` · `有重量`

### 氛围描述

整体是一份**可以打印出来带去谈判的技术报告**，不是一个 App 界面。

- **纸感底色**：页面底 `--bg #F6F7F9` 是带蓝调的冷灰白，取自蓝晒图纸张。不是纯白（刺眼、廉价），更不是奶油米色（暖中性色带一律读作"AI 温暖模板"，明令禁止）。
- **层级靠明度不靠投影**：卡片默认只有 1px 环（`--elev-ring`），无投影。深浅由 `surface / bg / surface-sunken` 三级表面区分。这是技术文档的语言，不是消费级 App 的语言。
- **数字是主角**：匹配度、年限、阶段时长全部走等宽字体 + `tabular-nums`。工程人对数字的敏感度远高于对图形的敏感度，让数字有"读数感"是建立可信度最便宜的手段。
- **强调色极度稀缺**：全站只有一个 `--accent`，每屏可见使用 ≤ 2 处。稀缺才有指向性——这正是"罗盘"的隐喻。

### 明确排除的氛围

不要科技炫光、不要玻璃拟态、不要渐变主视觉、不要消费级轻快感、不要插画、不要抽象 3D 图形。目标用户对这些的默认解读是"又一个割韭菜的"。

---

## 2. Color Palette & Roles

完整定义见 `design-tokens.json` / `tokens.css`。以下是角色与用法约束。

### A1-identity（品牌核心，不可省略）

| Token | 值 | 角色 | 对比度 |
|-------|-----|------|--------|
| `--bg` | `#F6F7F9` | 页面底。冷灰白，蓝晒图纸张 | — |
| `--surface` | `#FFFFFF` | 卡片/容器面 | — |
| `--fg` | `#10151C` | 主文本。近黑带蓝调，非纯黑 | 17.09:1 |
| `--muted` | `#5A6572` | 副文本、字段说明 | 5.54:1 |
| `--border` | `#DDE1E8` | 卡片默认边框 | — |
| `--accent` | `#14507D` | **Blueprint Blue** 品牌强调 | 7.92:1 |

**主色为什么是 `#14507D`**：

1. 刻意避开 Tailwind 默认 indigo 色带（`indigo-500` 一族）——那是业界公认的"一眼 AI"第一罪，本系统全色板不含该色带任何色值。
2. 蓝晒图深钢蓝，冷、深、有重量，与"专业可信"心智直接对应，与工程/基建语境同源。
3. 白字在其上对比度 8.49:1，作为文本色在 `--bg` 上 7.92:1，主 CTA 与链接双用途都不需要额外调色。

### 前景四级层级（全部达 WCAG AA）

`--fg` 17.09:1 → `--fg-2` 10.40:1 → `--muted` 5.54:1 → `--meta` 4.67:1

四级都在 4.5:1 以上，意味着**任何一级都可以安全承载正文**，层级是视觉设计手段而非可读性妥协。这一点很多设计系统做不到（三级灰通常已跌破 3:1）。

### A2 语义色

| Token | 值 | 语义 | 对比度 |
|-------|-----|------|--------|
| `--success` | `#0E7A54` | 工程验收绿：已达成、高匹配、成功 | 4.98:1 |
| `--warn` | `#9A5A0B` | 赭石琥珀：**最大差距项**、置信度偏低提示 | 5.10:1 |
| `--danger` | `#B42318` | 错误、删除数据、校验失败 | 6.13:1 |

每个语义色配一组 `-wash`（极淡底）与 `-border`（淡边框），用于构造提示卡而不使用饱和大色块。

### C-extension 数据可视化

`--data-1 #14507D` → `--data-2 #2E7DA6` → `--data-3 #5F89A3` + `--data-track #DDE1E8`

**同色系深浅递进，禁止彩虹色板。** 深浅本身就编码了匹配度排序。

> **数据色三重编码硬规则**：匹配度必须同时用「等宽数字 + 刻度条长度 + 文字档位（高度匹配 / 中等匹配 / 需较大跨度）」表达，**绝不允许仅靠颜色传达含义**。色盲用户与黑白打印场景都必须可读。

### Accent 预算表（每屏 ≤ 2 处可见饱和使用）

| 页面 | 允许的 accent 饱和使用 | 说明 |
|------|------------------------|------|
| 落地/输入页 | ① 提交按钮 ② 已勾选的隐私框 | 快速填入 chip 未选中态用中性色 |
| 诊断结果页 | ① sticky CTA 主按钮 ② 最匹配卡的 `ring-accent` | 匹配度条的 `data-1` 虽同值，但属图表子系统，不计入 |
| 学习路径 | ① 「可写进简历」chip | 交付物卡用 `accent-wash` 底 + `accent-border` 淡边框，不用饱和色 |
| 弹层 | ① 主按钮 | — |

`--accent-wash` 与 `--accent-border` 是低饱和派生，不计入预算——这是控制"强调色泛滥"的关键机制。

---

## 3. Typography Rules

### 字体栈决策（含工程约束）

| 角色 | 栈 | 加载方式 |
|------|-----|----------|
| `--font-display` | Archivo + 中文系统栈 | **自托管 latin 子集 woff2（约 30KB）**，`font-display: swap` |
| `--font-body` | 纯中文系统栈（PingFang SC / HarmonyOS Sans SC / Source Han Sans SC / Microsoft YaHei） | 零加载成本 |
| `--font-mono` | JetBrains Mono | **仅子集化数字与符号（约 8KB）** |

> **硬约束：禁止 `@import` Google Fonts。** 大陆网络 + 微信 X5 内核下会阻塞首屏渲染，直接击穿「首屏 < 3s」的 P0 指标。此条已同步架构师写入技术 Spec。

**为什么是 Archivo 而不是 Inter**：Inter 作为正文可用，但它是当前所有 AI 生成界面的默认展示字体，不具备任何辨识度。Archivo 源自美国标牌哥特体，有"铭牌 / 标题栏 / 基础设施"的气质，宽度略窄、字面开放，中英混排时与思源黑体的字面高度匹配良好。中文回落系统字体，Archivo 只承担西文与数字部分——这恰好是标题里最需要"工业感"的部分。

### 字号层级（8 级）

`12 / 14 / 16 / 18 / 20 / 24 / 32 / 40`（后三级用 `clamp` 流体，下限保守）

正文基准 16px，**不得更小**。目标用户 30-40 岁，夜间弱光环境使用，12px 只用于元数据。

### 字距（决定工艺的关键）

| 场景 | 字距 | Token |
|------|------|-------|
| 正文 14-18px | `0` | `--tracking-body` |
| 小字 12-13px | `0.01em` | `--tracking-small` |
| **section 短标签 / 全大写** | **`0.08em`（强制）** | `--tracking-caps` |
| 标题 20-24px | `-0.01em` | `--tracking-heading` |
| 大标题 ≥32px | `-0.02em` | `--tracking-display` |

### 字重三级

`400 Read` / `500 Emphasize` / `600 Announce`

不用 ≤300：微信安卓 X5 内核下细字重中文渲染发虚。不用 ≥700：过重，不符克制基调。

### 行高

中文正文 `1.65`（高于西文常用 1.5），长文档 `1.75`，标题 `1.25-1.4`。

### 数字的强制规则

所有匹配度、从业年限、阶段时长、报告编号、倒计时**必须**加 `.num` 类（`--font-mono` + `tabular-nums`）。三个赛道的匹配度数字必须纵向对齐——这是"仪器读数"氛围的核心承载，也是让数字可信的最低成本手段。

---

## 4. 图标系统（已锁定）

### 锁定：Lucide（ISC 协议，24×24 网格）

选型理由：描边一致性最好；`hard-hat` / `compass` / `milestone` / `gauge` 等工程语义图标齐全，正好贴产品；纯 SVG / React / Vue 三种消费方式都有。Tabler 图标多但描边粗细不稳，Heroicons 缺工程类语义。

### 尺寸与描边规范

| 尺寸 | Token | stroke-width | 用途 |
|------|-------|--------------|------|
| 16px | `--icon-sm` | **2** | 行内文字旁标记、表单内联提示、标签 |
| 20px | `--icon-md` | **1.75** | 按钮内、列表项、导航项（**默认档**） |
| 24px | `--icon-lg` | **1.5** | 卡片/区块标题、空状态、赛道卡 |

**为什么线宽随尺寸反向递增**：Lucide 的 `stroke-width` 定义在 24 的 viewBox 内，缩放时描边等比缩小。实际渲染线宽 = `strokeWidth × (size / 24)`。

- 若三档都用 1.5：渲染为 1.00 / 1.25 / 1.50px —— 小图标明显偏轻，贴在 16px 中文旁边会"发虚"
- 本方案 2 / 1.75 / 1.5：渲染为 **1.33 / 1.46 / 1.50px** —— 跨尺寸视觉重量近似恒定

此三档与架构师 Spec 第 8.1 章 `Icon.tsx` 的 `STROKE` 映射一致，前端直接消费，不得自定义中间值。

颜色一律 `currentColor`，不硬编码。全项目只用 Lucide 一套，不混用。

### 语义映射表（禁止随意换图标）

| 语义 | Lucide 名 | 语义 | Lucide 名 |
|------|-----------|------|-----------|
| 上传简历 | `upload` | 简历文件 | `file-text` |
| 表单填写 | `pencil-line` | 从业年限 | `calendar-clock` |
| 工作内容 | `hard-hat` | 目标方向 | `compass` |
| 隐私授权 | `shield-check` | 删除数据 | `trash-2` |
| 匹配度 | `gauge` | 赛道匹配 | `target` |
| 推理依据 | `quote` | 可迁移能力 | `repeat-2` |
| 最大差距 | `triangle-alert` | 学习路径 | `milestone` |
| 阶段交付物 | `package-check` | 加入社群 | `users` |
| 预约 1v1 | `calendar-check` | 导出 PDF | `download` |
| 分享链接 | `share-2` | 复制 | `copy` |
| 加载中 | `loader-2` | 成功 | `circle-check` |
| 错误 | `circle-alert` | 空态 | `inbox` |
| 展开 | `chevron-down` | 返回 | `chevron-left` |
| 关闭 | `x` | 链接失效 | `link-2-off` |

### 绝对禁令

**严禁任何 emoji 充当功能图标。** 本文档全文经正则 `[\x{1F300}-\x{1F9FF}\x{2600}-\x{26FF}\x{2700}-\x{27BF}]` 扫描为零命中，所有产出文件同此标准。

### 品牌 Logo Mark

自定义 SVG，不直接使用 Lucide `compass`。构型：**方形取景框内一枚指北针**——外框呼应图纸标题栏，内针呼应罗盘。单色 `--accent`，24px 版本需简化为「框 + 三角针」两个形状，保证 16px 下不糊。

---

## 5. Component Stylings

### Button

| 尺寸 | 高度 | 用途 |
|------|------|------|
| lg | 52px | 主提交（落地页「生成我的转型诊断」） |
| md | 44px | 常规，同时是触摸目标下限 |
| sm | 36px | 仅桌面工具栏，移动端不使用 |

| 变体 | 背景 | 文字 | 边框 |
|------|------|------|------|
| Primary | `--accent` | `--accent-on` | 无 |
| Secondary | 透明 | `--accent` | 1px `--accent` |
| Ghost | 透明 | `--fg-2` | 无（hover 时 `--surface-sunken` 底） |
| Danger | `--danger` | `#fff` | 无 |
| Danger-outline | 透明 | `--danger` | 1px `--danger-border` |

圆角 `--radius-sm`，内边距 `12px 20px`，字重 `--weight-announce`，过渡 `--motion-fast`。

### Input / Textarea

高度 44px（单行），textarea min-height 96px。`--surface` 底 + 1px `--border-strong` + `--radius-sm` + padding 12px。

- focus：边框转 `--accent` + `--focus-ring`
- error：边框转 `--danger` + `--focus-ring-danger` + 字段下方内联错误（`circle-alert` 16px + `--danger` 文字）
- **label 必须可见**，严禁用 placeholder 充当 label
- placeholder 必须是**真实工程场景表述**，禁止 Lorem ipsum 类占位

### Card

`--surface` 底 + `--elev-ring` + `--radius-md` + padding `--space-5`（移动）/ `--space-6`（桌面）。

**默认无投影。** 需要强调时用 `--elev-ring-accent`，不用彩色左边框（见禁令）。

### Chip / SegmentedControl

高度 40px，触摸区补足至 44px（用 `::before` 扩展热区）。

- 未选：`--surface` + 1px `--border` + `--fg-2`
- 选中：`--accent-wash` 底 + 1px `--accent` + `--accent` 文字 + **`check` 16px 图标**

选中态带 check 图标是**双编码**，不单靠颜色区分——色盲可用性硬要求。

### 匹配度刻度条（本产品标志性组件）

```
匹配度                                    78
┏━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃██████████████████████░░░░░░░░┃
┗━━━━━━┳━━━━━┳━━━━━┳━━━━━┛
       0     25    50    75   100
                            高度匹配
```

- 槽 `--data-track`，填充 `--data-1/2/3`，高度 8px，`--radius-xs`
- 刻度线：`--border-soft` 1px 竖线，位于 25/50/75
- 刻度标签：`--text-xs` + `.num` + `--meta`
- 读数：`--text-2xl` + `.num` + `--weight-announce`，右对齐
- 档位文字：`--text-sm` + `--muted`

**不用环形/饼图**，理由：移动端小尺寸下环形读数困难；三个赛道横向对比时条形一眼可比长短；刻度线直接呼应"水准仪"的品牌隐喻。

---

## 6. Layout Principles

### 容器

| Token | 值 | 用途 |
|-------|-----|------|
| `--container-reading` | 720px | **结果页、学习路径、长文档**。中文每行 25-37 字最佳阅读区 |
| `--container-app` | 1120px | 输入页桌面分屏壳 |

结果页刻意不用 1200px 宽栏——它是一份**长文档**，不是仪表盘。720px 让阅读节奏正确。

### 沟槽与节区节奏

| 断点 | gutter | section-y |
|------|--------|-----------|
| 手机 | 16px | 32px |
| 平板 ≥768 | 24px | 48px |
| 桌面 ≥1024 | 32px | 80px |

### 栅格

桌面 12 列 / 平板 8 列 / 手机 4 列。输入页桌面用 `grid-template-columns: 7fr 5fr`（非对称，VARIANCE 4）。

### 密度规则（VISUAL_DENSITY 6）

**关键：可迁移能力列表用 `divide-y` 行分隔，不是 7 个独立卡片。** 重复的等尺寸卡片网格是 AI 模板特征，且在密度 6 下浪费垂直空间。

赛道匹配允许用卡片（三者是并列决策选项，卡片语义成立），但**三张卡必须不等权重**：最匹配那张用 `ring-accent` + 「最匹配」chip + 依据全展开，另两张普通 ring + 依据默认展示 2 条。

---

## 7. Depth & Elevation

| 层级 | Token | 用法 |
|------|-------|------|
| 平 | `--elev-flat` | 页面内嵌块 |
| 环 | `--elev-ring` | **卡片默认**。只有环，无投影 |
| 强环 | `--elev-ring-strong` | 输入框 |
| 强调环 | `--elev-ring-accent` | 最匹配卡、选中态 |
| 浮起 | `--elev-raised` | 无边框元素，最大 blur 6px |
| 遮罩层 | `--elev-overlay` | Modal / ActionSheet，元素本身无边框 |
| 吸底 | `--elev-sticky` | 底部 CTA 条：顶部发丝线 + 轻上浮 |

> **禁止幽灵卡片**：1px 边框 与 blur ≥16px 投影**不得共存于同一元素**。ring 与 raised 二选一。

---

## 8. Do's and Don'ts

### 必须做

- 所有颜色走 Design Token，唯一例外 `#fff` / `#000`
- 匹配度三重编码（数字 + 条长 + 文字档位）
- 所有数字读数加 `.num`（等宽 + tabular-nums）
- `:focus-visible` 全局可见，触摸目标 ≥ 44×44px
- 每个异步组件覆盖 Loading / Empty / Error / Populated / Edge 五态
- 所有 placeholder / 示例文案使用真实工程场景表述
- 实现 `prefers-reduced-motion` 降级

### 绝对禁止（出现即重写）

| # | 禁令 | 本项目的替代方案 |
|---|------|------------------|
| 1 | emoji 充当功能图标 | Lucide 语义映射表 |
| 2 | 紫→粉渐变主视觉 | 无渐变。纯色 + 表面明度递进 |
| 3 | 硬编码颜色值 | `var(--token)` |
| 4 | 空洞占位文案（"Welcome to" / Lorem ipsum） | 真实工程表述 |
| 5 | 千篇一律 Hero（大标题+居中 CTA+抽象 3D） | 首屏直接放真实的「翻译对照」实例卡 + 可填表单 |
| 6 | 彩色左边框（`border-left > 1px` 强调） | 用表面明度（`surface` vs `surface-sunken`）区分 |
| 7 | 渐变文字（`background-clip: text`） | 纯色 + 字重/字号强调 |
| 8 | 毛玻璃装饰 | 不使用 |
| 9 | 幽灵卡片（1px 边框 + blur ≥16px） | ring 与 raised 二选一 |
| 10 | 过度圆角 ≥24px | 上限 14px |
| 11 | 等尺寸卡片无限重复 | `divide-y` 列表；卡片必须不等权重 |
| 12 | 每个 section 顶部都挂小型大写标签 | 仅结果页三大模块使用，落地页不用 |
| 13 | 编号 section 标记（`01 · 关于`） | 仅学习路径三阶段使用（此处编号有真实语义：时间顺序） |
| 14 | 虚构指标（"10000+ 用户信赖"） | 不写任何未经证实的数字 |
| 15 | 奶油/米色背景 | 冷灰白 `#F6F7F9` |
| 16 | 绝对化表述（"保证入职""薪资翻倍""100% 匹配"） | 文案红线，广告法风险 |

---

## 9. Responsive Behavior

### 断点（移动优先，一律 `min-width`）

| 断点 | 宽度 | 设备 | 布局 |
|------|------|------|------|
| xs | 320-359 | 小屏 iPhone SE | 单列，**必须不横向滚动** |
| sm | ≥360 | 主流手机（设计基准 **375**） | 单列 |
| md | ≥768 | 平板 | 单列加宽 / 部分双列 |
| lg | ≥1024 | 笔记本 | 输入页启用 7:5 分屏 |
| xl | ≥1280 | 桌面（设计基准） | 完整布局 |

### 微信内置浏览器专项（P0，主战场）

| 问题 | 处理 |
|------|------|
| `100vh` 被地址栏顶掉 | 用 `.h-screen-safe` / `.h-screen-fixed` 工具类（内含 `100vh` → `100dvh` 层叠回退）。**严禁裸写 `100dvh`**——`dvh` 需 Chrome 108+，低于此版本整条声明被丢弃导致高度塌缩，与 ADR-002 的 Chrome 88 级内核下限冲突 |
| 深色模式强制反色破坏色阶 | `<meta name="color-scheme" content="light">` + `:root { color-scheme: light }`，V1 锁浅色 |
| 字号漂移 | `-webkit-text-size-adjust: 100%` |
| 底部安全区 | `env(safe-area-inset-bottom)`，viewport 需 `viewport-fit=cover` |
| X5 内核细字重发虚 | 字重下限 400 |
| Google Fonts 阻塞 | 中文走系统栈，西文自托管子集 |
| 缩放 | **不得设 `user-scalable=no`**（无障碍红线） |

### 触摸

最小目标 44×44px，按钮间距 ≥8px。桌面 hover 效果必须有非 hover 的等价触发路径（移动端无 hover）。

---

## 10. 页面清单与设计说明

| # | 页面 | 路由 | 优先级 |
|---|------|------|--------|
| 1 | 落地 / 输入页 | `/` | P0 |
| 2 | 诊断进行中 | `/diagnosing`（或同页状态） | P0 |
| 3 | 诊断结果页（核心） | `/report/:id` | P0 |
| 4 | 学习路径（结果页内模块） | `/report/:id#path` | P0 |
| 5 | 导出 / 留资弹层 | 覆盖层 | P0 |
| 6 | 删除数据确认 | 覆盖层 | P0 |
| 7 | 只读分享视图 | `/s/:token` | P0 |
| 8 | 错误 / 降级页 | `/error`、内联 | P0 |
| 9 | 隐私政策 / 用户协议 | `/privacy`、`/terms` | P0 |
| 10 | 简历优化对比 | `/report/:id#rewrite` | P1（V1.1） |

---

### 10.1 落地 / 输入页 `/`

#### 设计要点

**首屏不做 Hero，首屏就是"证据 + 表单"。** 用户从小红书带着焦虑进来，5 秒内必须回答两件事：这是给我的吗？我要付出什么？

移动端自上而下：

1. **顶栏 56px**（sticky）：Logo mark + 「转型罗盘」。**不放导航**——减少跳出路径。
2. **人群自证行**：`hard-hat` 16px + 「房建 · 市政 · 机电 · 基建」，`.label-caps` 样式。这一行的作用是让目标用户 3 秒内确认"是给我的"。
3. **定位陈述**（左对齐，不居中）：
   - H1 `--text-3xl`：「把你的工地经验，翻译成新赛道听得懂的语言」
   - 副句 `--text-base` `--muted`：「填 3 个空，5 分钟，免费看完整诊断结果。不用注册。」
   - 副句的三个短句直接消解三种门槛焦虑：工作量、时间、注册。
4. **真实产品内容预览卡**（替代千篇一律 Hero 的关键）：
   - 上半 `--surface-sunken` 底，`.label-caps` 标「简历原文」，内容：「负责 5.2 万㎡住宅项目主体结构施工，协调甲方、监理及 6 家分包单位」
   - 中间 `arrow-down` 20px `--muted`（桌面为 `arrow-right`）
   - 下半 `--surface` 白底 + ring，标「储能 EPC 项目管理语言」，内容：「多方接口管理与进度履约：统筹业主、监理及 6 家承包方，交付 5.2 万㎡工程节点」
   - 底部 `--meta` `--text-xs`：「这是产品实际输出的一条改写，来自一位 12 年房建从业者的脱敏样本」
5. **输入卡**（主路径）：
   - 字段 1「从业年限」：**分段选择器**而非输入框。选项 `< 2 年 / 2-5 年 / 5-8 年 / 8-12 年 / 12 年以上`（5 个，含 <2 年边界）。移动端 2 列 wrap。
   - 字段 2「主要工作内容」：textarea 4 行。placeholder 为真实表述：「例：房建总包项目技术负责人，管过 3 个高层住宅项目，负责施工方案编制、进度计划、分包管理和竣工验收资料」。
     - **下方 3 个「快速填入」chip**：`房建施工管理` / `市政工程` / `机电安装`。点击填入可改写的模板文本。这是转化关键——大量用户卡在"不知道怎么写自己"。
     - 字数引导用**正向**文案：「再写 20 字，诊断会更准」，不是负向报错。
   - 字段 3「想去的方向」：单选 chip，`新能源与储能` / `智能建造与 BIM` / `工程类 B2B 出海` / **`我还不知道`**。
     - 「我还不知道」与其他三项**等权重甚至更醒目**——80% 主用户会选它。选中后下方出现 `--meta` 一行：「没关系，这正是罗盘要回答的」。
     - 4 个选项，符合工作记忆 ≤4 项。
6. **次路径（上传简历）**：「或者」+ hairline 分隔。移动端为按钮，桌面为可拖拽 dropzone。
   - 文案：「上传简历（PDF / Word，10MB 内）」
   - 关键信任文案 `--meta`：「上传后我们会先在你的浏览器里抹掉姓名、电话、身份证和公司名，再送去分析」
7. **隐私授权块**（独立、显眼，绝不做成灰色小字）：
   - 独立卡片。未勾选：`--surface` + `--border`；已勾选：`--accent-wash` + `--accent` 边框
   - `shield-check` 20px + checkbox 24×24（触摸区 44×44）+ **默认不勾选**
   - 主文案 `--text-base`（16px，与正文同级）`--fg`：「我同意将上述经历用于生成本次诊断」
   - 说明 `--text-sm` `--muted`：「我们不会用于模型训练，不会对外提供。数据默认 30 天自动清除，你也可以在结果页随时一键删除。」
   - 《隐私政策》《用户协议》两个 `--accent` 链接，独立文档
8. **提交按钮**：全宽 52px，Primary，`compass` 16px + 「生成我的转型诊断」
   - **按钮不禁用**。禁用按钮不告诉用户原因，是新手用户第一红旗。未满足条件时点击 → 平滑滚动到第一个缺项 + 高亮 + 内联错误。
   - 下方 `--meta` 居中：「免费 · 无需注册 · 约 30 秒」
9. **页脚**：隐私政策 / 用户协议 / 免责声明。

桌面 ≥1024：`--container-app` 1120px，`grid: 7fr 5fr`，gap 48px。左列为陈述 + 表单 + 隐私 + 提交；右列 sticky（top 88px）放真实产品内容预览卡 + 一条缩略匹配度刻度条示意。滚动时右列固定造成非对称节奏（VARIANCE 4）。

#### 状态

| 状态 | 表现 |
|------|------|
| Default | 如上 |
| 字段校验错误 | 字段下方内联，`circle-alert` 16px + `--danger` |
| 隐私未勾选 | 整块转 `--danger-border`，抖动 1 次（reduced-motion 下仅变色不抖动），滚动定位 |
| 上传中 | 文件名 + 进度条 + 取消按钮 |
| 解析失败 | `--danger-wash` 卡：「这份文件读不出文字（可能是扫描件或加密 PDF）」+ 主按钮**「改用表单填写」**（一键切换，不留死胡同）+ 次按钮「换个文件」 |
| 提交中 | 按钮转 loading，`loader-2` 旋转 + 「正在解析…」，表单锁定 |

#### 前端还原提示词

> 构建移动优先的落地输入页。容器：移动 100% + 16px gutter，≥1024px 切 `--container-app` 1120px 的 `grid-template-columns: 7fr 5fr`（gap 48px，右列 `position: sticky; top: 88px`）。
>
> 垂直节奏用 `--section-y`（移动 32px / 桌面 80px），卡片间距 `--space-5`。全部左对齐，**不要任何居中大标题布局**。
>
> 顶栏 56px sticky，`--surface` 底 + 底部 1px `--border`，只放 logo 与产品名，无导航。
>
> 首屏顺序：人群自证行（`hard-hat` 16px + `.label-caps`）→ H1（`--text-3xl`，`--font-display`，`--tracking-display`）→ 副句（`--text-base`，`--muted`）→ **翻译对照预览卡**（上半 `--surface-sunken`、下半 `--surface` + `--elev-ring`，中间 `arrow-down` 20px）→ 输入卡 → 隐私块 → 提交按钮。
>
> 输入卡内：分段选择器（5 项，移动 2 列 grid，40px 高，触摸区补 44px，选中态 `--accent-wash` + `--accent` 边框 + `check` 16px）；textarea（min-height 96px，`--border-strong` 边框，focus 转 `--accent` + `--focus-ring`，placeholder 用真实工程表述）；快速填入 chip 组（3 个，Ghost 样式）；方向单选 chip（4 个，含「我还不知道」）。
>
> 隐私块是独立卡片，checkbox 24×24、触摸区 44×44、**默认 unchecked**，主文案 16px 不是小字，勾选后整卡转 `--accent-wash` 底 + `--accent` 边框，`--motion-fast` 过渡。
>
> 提交按钮 52px 全宽 Primary，**始终 enabled**；点击时若校验不过，`scrollIntoView({behavior:'smooth', block:'center'})` 到第一个缺项并加 `--danger` 边框 + 内联错误。
>
> 所有颜色用 `var(--token)`，禁止硬编码。图标全部 Lucide，`currentColor`，线宽按档位取 16px→2 / 20px→1.75 / 24px→1.5。**不要 emoji，不要渐变，不要毛玻璃，不要抽象 3D 图形。**
>
> 状态覆盖：default / 字段错误 / 隐私未勾 / 上传中 / 解析失败（含「改用表单填写」一键切换）/ 提交中。

---

### 10.2 诊断进行中

#### 设计要点

**不做全屏 spinner。** AI 端到端 < 30s，超过 10s 无反馈流失率陡增，必须给真实进度。

保留页面壳，中间是**分步清单**（四步，`divide-y`）：

1. 解析经历
2. 提取可迁移能力
3. 匹配目标赛道
4. 生成学习路径

每步三态图标：待处理 `circle`（`--meta`）/ 进行中 `loader-2` 旋转（`--accent`）/ 完成 `circle-check`（`--success`）。切换 `--motion-base` 200ms。

进行中步骤下方显示**真实子信息**（如「已识别 4 段项目经历」），不是假进度百分比。欺骗性进度条会在结果不准时反噬信任。

底部 `--meta`：「通常 20-30 秒。请不要关闭页面。」

超过 30s：追加一块「还在算。要不要留个邮箱，好了推给你？」+ 邮箱输入。

#### 状态

| 状态 | 表现 |
|------|------|
| 进行中 | 分步清单，逐步打勾 |
| 超时（>30s） | 追加留邮箱块 |
| 失败 | 切换错误卡：`circle-alert` 24px `--danger` + 明确说明 + 主「重试」+ 次「留邮箱，好了推给我」。**绝不白屏、绝不无限加载** |
| reduced-motion | `loader-2` 不旋转，改静态图标 + 文字「进行中…」 |

#### 前端还原提示词

> 诊断进行中页。沿用顶栏，内容区 `--container-reading` 720px 居中，垂直方向距顶 `--space-16`。
>
> 核心是一个四步清单卡（`--surface` + `--elev-ring` + `--radius-md`），步骤间用 1px `--border-soft` 分隔（`divide-y`），每行高 56px，padding `--space-4`。
>
> 每行左侧 20px 状态图标 + 右侧步骤名（`--text-base`）。三态：待处理 `circle` `--meta`；进行中 `loader-2` `--accent` 旋转 1s linear infinite；完成 `circle-check` `--success`。状态切换 `--motion-base` `--ease-standard`。
>
> 进行中的那一行下方追加一行 `--text-sm` `--meta` 子信息（来自服务端真实事件流，如「已识别 4 段项目经历」）。**不要用百分比假进度条。**
>
> 卡片下方 `--space-4` 处一行 `--text-sm` `--meta` 居中提示。计时超过 30s 时，在卡片下方插入留邮箱块（输入框 + 「提交」次按钮），`--motion-base` 淡入。
>
> 失败时整卡替换为错误态：`circle-alert` 24px `--danger` + 标题 + 说明 + 两个按钮（Primary「重试」/ Secondary「留邮箱，好了推给我」）。技术错误码放进折叠的「技术详情」内，`--meta` + `--font-mono`，默认收起。
>
> `prefers-reduced-motion` 下 `.spinner` 停止旋转，改为静态 `loader-2` + 文字「进行中…」。

---

### 10.3 诊断结果页 `/report/:id`（核心）

#### 设计要点

容器 `--container-reading` 720px。这是一份**长技术文档**。

**最关键的设计决策：推理依据是卡片主体，百分比只是右上角读数。** 这直接对治 Apt AI「推荐得莫名其妙」的差评，也是 PIPL 第 24 条可解释性的落地。

##### 模块 0：报告头

- `file-text` 16px + 「转型诊断报告」+ 报告编号（`.num` `--meta`）
- 画像摘要 `--text-lg` `--fg`：「12 年 · 房建施工与项目管理 · 目标方向：待确定」
- `--meta` `.num`：「生成于 2026-08-03 21:40 · 30 天后自动清除」
- **`trash-2` 16px + 「删除我的数据」**（Danger 文字按钮，**常驻**）。移动端置于报告头底部独立一行，不挤压。

##### 模块 1：可迁移能力（5-8 条）

- 标题：`repeat-2` 20px + 「可迁移能力」+ 计数 chip「7 项」（pill）
- **列表用 `divide-y`，不是 7 张卡片**（密度 6 规则）
- 每行：
  - 能力名（`--text-base` `--weight-announce`）
  - 强度：5 格方块刻度 + 文字档位（双编码，不用星星——工程感取方块）
  - **来源经历**：`--surface-sunken` 底 + `quote` 16px + `--text-sm`，引用用户原文片段：「协调甲方、监理及 6 家分包单位」
  - 映射说明 `--text-sm` `--muted`：「对应 EPC 项目的多方接口管理」
- 默认展示 5 条，「展开全部 7 项」（`chevron-down`）

##### 模块 2：赛道匹配（3 张卡，核心中的核心）

- 标题：`target` 20px + 「赛道匹配」+ `--meta`「基于你的 4 段经历」
- **三卡不等权重**：
  - 第 1 张：`--elev-ring-accent` + 「最匹配」chip + 依据全展开
  - 第 2、3 张：`--elev-ring` + 依据默认 2 条 + 可展开

单卡结构：

```
┌──────────────────────────────────────┐
│ [最匹配]                               │  pill chip, --accent
│ 新能源与储能                            │  --text-xl / announce
│ 储能 EPC 项目管理 · 电站建设             │  --text-sm / --muted
│                                        │
│ 匹配度                            78   │  .num --text-2xl
│ ██████████████████████░░░░░░░░         │  刻度条
│ 0        25       50       75     100  │  .num --text-xs --meta
│                              高度匹配   │  --text-sm --muted
│                                        │
│ ── 为什么是这个方向 ─────────────       │  分隔标题 .label-caps
│                                        │
│  ↳ 你写的：「协调甲方、监理及 6 家       │  --surface-sunken 引用块
│     分包单位」                          │
│     对应：储能电站 EPC 的多方接口管理，  │  --text-sm --muted
│     是该岗位第一核心职责                 │
│                                        │
│  ↳ 你写的：「5.2 万㎡主体结构进度管控」   │
│     对应：储能电站土建与设备安装的       │
│     交叉进度管理                        │
│                                        │
│ ── 岗位参考 ──                         │
│ 储能 EPC 项目经理 · 电站建设主管         │
└──────────────────────────────────────┘
```

依据区占卡片 60% 以上高度——这是刻意的信息层级设计。

##### 模块 3：最大差距项（2 条）

- 标题：`triangle-alert` 20px `--warn` + 「最大差距」
- `--warn-wash` 底 + `--warn-border`
- 每条：差距名（announce）+ 为什么是差距 + 「怎么补」锚点跳转至学习路径对应阶段
- **措辞红线**：不写"你缺 X 就进不去"，写"这一项在目标岗位 JD 中出现频率高"

##### 模块 4：学习路径（详见 10.4）

##### 模块 5：免责声明（常驻，不折叠）

`--surface-sunken` + `--text-sm` + `--meta`：

> 本报告由 AI 生成，为辅助参考，不构成职业中介服务、就业承诺或投资建议。诊断基于你提供的信息，建议结合行业实际与专业意见判断。
> 依据《个人信息保护法》第 24 条，你有权要求对本结论作出说明，也有权不接受完全由自动化决策产生的建议。

附「预约 1v1 人工复核」链接——把合规要求转成转化入口。

##### 模块 6：数据管理条

`shield-check` 20px + 「你的数据」+ 「30 天后自动清除」+ 「立即删除」（Danger-outline）

##### 模块 7：底部 sticky CTA（关键：不遮挡结果）

- 高度 68px + `env(safe-area-inset-bottom)`，`--surface` + `--elev-sticky`，`--z-sticky-cta`
- 两按钮：主「加入转型社群」（`users` 16px，flex 1.4）+ 次「预约 1v1 解读」（`calendar-check` 16px，flex 1），gap 8px，各 44px 高
- **`body` 必须有 `padding-bottom: calc(68px + var(--safe-b) + 16px)`** —— 保证内容永不被遮挡
- **延迟出现**：首次进入后滚动超过 20% 才淡入。让用户先看到结果，不是一进门就被推销
- **滚动到底部时 un-stick**：页面内嵌完整 CTA 区进入视口时，sticky 条淡出。消除永久遮挡感

页面底部内嵌完整 CTA 区：

- 主：「加入工程人转型社群（免费）」+ 说明「和同样在转型的工程人待在一起，不收费。」
- 次：「预约 1v1 转型解读」+ 说明「30 分钟，由有 10 年工程管理经验的顾问看你的报告。按提交顺序排期。」
- 第三行：`download` 导出 PDF / `share-2` 生成分享链接（触发轻登录弹层）

> **文案红线**：不写任何未经证实的数字（如"200+ 人在群里"）。「按提交顺序排期」是真实产能约束的诚实表达。

#### 状态

| 状态 | 表现 |
|------|------|
| Loading | 骨架屏，**按真实结构**：报告头骨架 + 3 条能力行骨架 + 3 张卡骨架。不用通用灰块 |
| Populated | 如上 |
| 模块级降级 | 某模块未产出：该模块显示「这一项没能生成」+ 「重试」按钮，其余模块正常展示 |
| Error | 整页失败 → 错误页 |
| Edge：与工程无关 | 顶部 `--warn-wash` 提示条：「你的经历与工程/基建关联度较低，当前版本主要覆盖房建、市政、机电、基建背景，以下结论仅供参考」 |
| Edge：年限 < 2 年 | 报告头加 chip「样本较少，结论置信度偏低」（`--warn`） |
| Edge：三赛道均 < 40 | **不硬凑**。显示「你的经历与当前覆盖的 3 个赛道跨度都较大」+ 说明 + 引导 1v1。宁可承认覆盖不足，不瞎编 |
| Edge：超长简历 | `--meta` 提示「已分析你最近的 5 段经历」 |

#### 前端还原提示词

> 诊断结果页。容器 `--container-reading` 720px 居中，移动端 100% + 16px gutter。整页是长文档节奏，模块间距 `--section-y`。
>
> `body` 必须设 `padding-bottom: calc(var(--cta-bar-h) + var(--safe-b) + var(--space-4))`。
>
> **模块 1（可迁移能力）用 `divide-y` 列表，绝对不要做成 7 张等尺寸卡片。** 每行 padding `--space-4` 0，行间 1px `--border-soft`。行内：能力名（16px/600）+ 5 格方块刻度（每格 12×12px，`--radius-xs`，已填 `--data-1`、未填 `--data-track`，gap 4px）+ 文字档位 + 来源引用块（`--surface-sunken`，`--radius-sm`，padding `--space-3`，前置 `quote` 16px `--meta`）。
>
> **模块 2（赛道匹配）三张卡必须不等权重**：第一张 `--elev-ring-accent` + 「最匹配」pill chip（`--accent` 实心）+ 依据全展开；另两张 `--elev-ring` + 依据默认 2 条 + 「展开」按钮。
>
> 匹配度刻度条：槽 `--data-track` 高 8px `--radius-xs`；填充按排名取 `--data-1/2/3`；25/50/75 处插 1px `--border-soft` 竖线；刻度标签 `--text-xs` + `.num` + `--meta`；读数 `--text-2xl` + `.num` + 600 右对齐；下方文字档位 `--text-sm` `--muted`。**必须数字+条长+文字三重编码，禁止只靠颜色。禁止环形图。**
>
> 依据区是卡片主体，占卡高 60% 以上。每条依据：`↳` 前缀 + 「你写的：」+ 用户原文（`--surface-sunken` 引用块）+ 「对应：」+ 映射说明（`--text-sm` `--muted`）。
>
> 模块 3 用 `--warn-wash` 底 + 1px `--warn-border` + `--radius-md`，标题图标 `triangle-alert` `--warn`。
>
> 底部 sticky CTA：`position: fixed; bottom: 0`，高 68px + `padding-bottom: var(--safe-b)`，`--surface` + `--elev-sticky`，`z-index: var(--z-sticky-cta)`。滚动超过 20% 才 `--motion-base` 淡入；用 IntersectionObserver 监听页内完整 CTA 区，进入视口时淡出 sticky 条。
>
> 「删除我的数据」按钮常驻报告头，Danger-outline 或 Danger 文字按钮，点击开确认 Modal。
>
> 骨架屏必须按真实结构分块，不要通用灰条。所有数字加 `.num`。所有颜色 `var(--token)`。图标全 Lucide。**不要 emoji、不要渐变、不要彩色左边框、不要环形图。**

---

### 10.4 学习路径（结果页内模块 / `#path`）

#### 设计要点

- 标题：`milestone` 20px + 「三阶段路径」+ `--meta`「每个阶段结束，你手上要多一件能写进简历的东西」
- 移动：垂直时间轴（左侧 2px `--border` 竖线 + 节点圆点）；桌面：3 列等宽 grid + 顶部水平连接线
- **信息层级必须反转：交付物是主角，学习动作是配角。** PM 明确要求"落到交付物，非学 XX 课程"。

单阶段结构：

```
① 0-1 月                          阶段标签 .num + --text-sm
打通语言关                          --text-lg / announce

┌─ 阶段交付物 ─────────────────┐   accent-wash 底 + accent-border
│ [package-check]                │
│ 一份分布式储能 EPC 项目投标      │   --text-base / announce / --fg
│ 技术方案（脱敏后可写进简历）      │
│ [可写进简历]                    │   pill chip, --accent 实心
└────────────────────────────┘

支撑动作                            .label-caps
· 摸清储能 EPC 与房建总包的流程       divide-y 列表
  差异，重点看并网验收
· 找 2 份真实储能 EPC 招标文件通读
· 把你现有的投标方案改写 1 份

投入参考   每周 4-6 小时             --meta + .num
```

**交付物卡不用彩色左边框**（禁令 6），用 `--accent-wash` 底 + 1px `--accent-border` 淡边框。整卡内唯一的饱和 accent 是「可写进简历」chip——这样 accent 预算受控。

#### 状态

Loading 骨架 / 单阶段生成失败则该阶段降级为「这一阶段没能生成」+ 重试 / 差距项锚点跳转需高亮目标阶段 1.5s（`--accent-wash` 闪一次，reduced-motion 下改为静态描边）。

#### 前端还原提示词

> 学习路径模块。移动端垂直时间轴：容器 `padding-left: 28px`，绝对定位一条 2px `--border` 竖线在 `left: 8px`，每阶段起点放一个 12px 圆点（`--surface` 底 + 2px `--accent` 环）。桌面 ≥768 切 `grid-template-columns: repeat(3, 1fr)`，gap `--space-6`，顶部一条 2px `--border` 水平线连接三个节点。
>
> 每阶段块：阶段标签（`①` + 「0-1 月」，`.num` + `--text-sm` + `--meta`）→ 阶段目标（`--text-lg`/600）→ **交付物卡** → 支撑动作列表 → 投入参考。
>
> **交付物卡是视觉主角**：`--accent-wash` 底 + 1px `--accent-border` + `--radius-md` + padding `--space-4`。内含 `package-check` 20px + 交付物描述（`--text-base`/600/`--fg`）+ 「可写进简历」pill chip（`--accent` 实心 + `--accent-on` 文字，`--text-xs`）。**不要用 border-left 彩条强调。**
>
> 支撑动作用 `divide-y` 列表（1px `--border-soft`），`--text-sm` `--fg-2`，每行 padding `--space-3` 0。**不要给每条动作套卡片。**
>
> 支持从「最大差距项」锚点跳入：`scrollIntoView` 后目标阶段 `--accent-wash` 高亮 1.5s 再淡出；`prefers-reduced-motion` 下改为静态 2px `--accent` 描边保持 3s。

---

### 10.5 简历优化对比（P1 / V1.1）`#rewrite`

#### 设计要点

- 移动：上下堆叠。上「简历原文」（`.label-caps` `--meta`）+ `--surface-sunken` 卡；中 `arrow-down` 20px 居中 + 两侧 hairline；下「目标岗位语言」（`.label-caps` `--accent`）+ `--surface` 白卡 + ring
- 桌面：`grid: 1fr 40px 1fr`，中间 `arrow-right`
- **用表面明度区分前后，不用彩色边框**（禁令 6）
- 每条下方一行 `--meta` `--text-sm`「改了什么」：「把『负责』换成了可量化的交付动词，补了业主与分包的数量」——让用户学会方法，不只是拿到结果
- 改写卡右上 `copy` 16px 按钮，点击 toast「已复制」
- 至少 3 条，条目间 `divide-y`

#### 状态

Loading 骨架 / 单条失败降级 + 重试 / 复制成功 toast（`--z-toast`，2s 自动消失）/ 超长文本不截断，允许卡片自然增高。

#### 前端还原提示词

> 简历改写对比模块。移动端上下堆叠、桌面 ≥768 切 `grid-template-columns: 1fr 40px 1fr` 且 `align-items: center`。
>
> 每组对比：左/上为原文（`.label-caps` 标签「简历原文」`--meta`，卡片 `--surface-sunken` + `--radius-md` + padding `--space-4`，文字 `--text-sm` `--fg-2`）；中间箭头（移动 `arrow-down`、桌面 `arrow-right`，20px `--muted`）；右/下为改写（`.label-caps` 标签「目标岗位语言」`--accent`，卡片 `--surface` + `--elev-ring`，文字 `--text-base` `--fg`）。
>
> **严禁用 border-left 彩条区分前后，只用表面明度。**
>
> 改写卡右上角放 `copy` 16px 的 Ghost 图标按钮（触摸区 44×44，需 `aria-label="复制改写后的表述"`），点击后 toast「已复制」。
>
> 每组下方一行「改了什么」说明（`--text-sm` `--meta`）。组间 1px `--border-soft` 分隔，间距 `--space-6`。

---

### 10.6 导出 / 留资弹层

#### 设计要点

触发：点击「导出 PDF」或「生成分享链接」。移动端底部 ActionSheet（仅上两角 `--radius-lg`）；桌面居中 Modal 480px。

- 标题「留个手机号，好把报告存下来」
- 说明 `--text-sm` `--muted`：「用于找回这份报告。我们不会打电话推销。」——直接消除最大顾虑
- 手机号输入：`type="tel"` + `inputmode="numeric"`，44px 高
- 验证码输入 + 「获取验证码」按钮（60s 倒计时用 `.num`）
- 隐私说明一行 + 链接
- 主按钮「保存并导出」

#### 状态

Default / 号码格式错误（内联）/ 验证码发送中 / 倒计时中（按钮禁用 + `.num` 读秒）/ 验证码错误 / 提交中 / 成功（切换成功态 `circle-check` `--success` + 下载自动开始）/ 发送失败（可重试）。

关闭路径三条：右上 `x` 24px（触摸区 44）、点击遮罩、Esc 键。

#### 前端还原提示词

> 留资弹层。`< 768px` 渲染为底部 ActionSheet：`position: fixed; bottom: 0; width: 100%`，`border-radius: var(--radius-lg) var(--radius-lg) 0 0`，`padding-bottom: calc(var(--space-6) + var(--safe-b))`，进出用 `translateY` + `--motion-slow` `--ease-out`。`≥768px` 渲染为居中 Modal，宽 480px，`--radius-lg`。两者均 `--surface` 底 + `--elev-overlay`（**无边框**，避免幽灵卡片）。
>
> 遮罩 `rgba(16,21,28,.45)`，`--z-backdrop`；弹层 `--z-modal`。
>
> 内容：标题（`--text-xl`/600）→ 说明（`--text-sm` `--muted`）→ 手机号 input（`type="tel"` `inputmode="numeric"` `autocomplete="tel"`，44px）→ 验证码行（input flex 1 + 「获取验证码」Secondary 按钮，倒计时文字用 `.num`）→ 隐私说明 + 链接 → 主按钮全宽 44px。
>
> 无障碍：`role="dialog"` `aria-modal="true"` `aria-labelledby`；打开时焦点移入首个输入框并做 focus trap；Esc 关闭；关闭后焦点归还触发按钮。
>
> 状态：default / 格式错误（内联 `--danger`）/ 发送中 / 倒计时 / 验证码错误 / 提交中 / 成功（整体切换为 `circle-check` `--success` + 「已保存」+ 自动触发下载）/ 发送失败。

---

### 10.7 删除数据确认

#### 设计要点

危险操作，**用 Modal 不用 ActionSheet**（更重的确认仪式）。

- `triangle-alert` 24px `--danger`
- 标题「确定删除这份报告的全部数据？」
- 说明：「删除后，简历原文和诊断结果将在 24 小时内从服务器彻底清除，已生成的分享链接会立即失效。此操作不可撤销。」
- 按钮：「取消」（Secondary，右）/「确认删除」（Danger 实心，左）
- **默认焦点在「取消」上**——危险操作不应落在惯性确认位

#### 状态

Default / 删除中（按钮 loading，全局锁定）/ 成功（toast「数据已删除」+ 跳转落地页）/ 失败（内联错误 + 可重试）。

#### 前端还原提示词

> 删除确认 Modal。居中，宽 400px（移动端 `calc(100% - 32px)`），`--surface` + `--radius-lg` + `--elev-overlay`，padding `--space-6`。
>
> 顶部 `triangle-alert` 24px `--danger`（`--danger-wash` 圆底 40×40 承托）→ 标题（`--text-lg`/600）→ 说明（`--text-sm` `--fg-2` `--leading-body`）→ 按钮行（`display:flex; gap: var(--space-3)`，各 44px）。
>
> 「确认删除」为 Danger 实心（`--danger` 底 + `#fff` 字），「取消」为 Secondary。**打开时 `autofocus` 必须落在「取消」上**，不是删除按钮。
>
> `role="alertdialog"` `aria-modal="true"`，focus trap，Esc 等同取消。删除中禁用两个按钮并在主按钮内显示 `loader-2`。

---

### 10.8 只读分享视图 `/s/:token`

#### 设计要点

- 顶部提示条（`--accent-wash`，非 sticky）：`link` 16px + 「这是一份只读分享报告 · 剩余 5 天有效」（天数 `.num`）
- 内容复用结果页，但**移除**：删除数据按钮、导出按钮、编辑入口、报告 ID 可操作部分
- **移除所有 PII**：画像摘要仅保留「12 年 · 房建施工与项目管理」，不含姓名、公司名；依据引用中的「你写的」改为「原表述」且已脱敏
- **CTA 改为单一**：「测测我自己的转型方向」→ 跳落地页。对访客推 1v1 是错的，裂变逻辑才对
- 底部：「这份报告由转型罗盘生成」+ logo
- 微信适配：`-webkit-text-size-adjust: 100%`、无横向滚动、不依赖图片（微信会压缩）

#### 状态

| 状态 | 表现 |
|------|------|
| Loading | 骨架屏 |
| Populated | 只读结果 |
| **链接过期** | 独立页：`link-2-off` 32px `--meta` + 「这个分享链接已过期」+ 说明「分享链接有效期为 7 天」+ 主按钮「测测我自己的转型方向」 |
| 链接不存在 | 同版式，文案「这个链接无效」 |
| 已被删除 | 同版式，文案「报告所有者已删除这份数据」 |

#### 前端还原提示词

> 只读分享页。复用结果页组件树，通过 `readonly` 模式关闭所有写操作入口（删除、导出、留资、重试）。
>
> 顶部插入非 sticky 提示条：`--accent-wash` 底 + 1px `--accent-border`，`--radius-md`，padding `--space-3` `--space-4`，内容 `link` 16px + `--text-sm` `--fg-2`，剩余天数用 `.num`。
>
> 数据层必须走脱敏后的 DTO：画像摘要不含姓名/公司名；依据引用标签由「你写的」改为「原表述」。**前端不得渲染任何未脱敏字段。**
>
> 底部 CTA 区只保留一个 Primary 全宽按钮「测测我自己的转型方向」→ `/`，**不出现 1v1 与社群入口**。下方一行 `--meta` 品牌署名。
>
> 过期/无效/已删除三种终态共用一个版式组件：居中，`link-2-off` 或 `circle-alert` 32px `--meta`，标题 `--text-lg`，说明 `--text-sm` `--muted`，Primary 按钮「测测我自己的转型方向」。**不要白屏，不要 404 默认页。**
>
> 微信适配：`html { -webkit-text-size-adjust: 100% }`，`overflow-x: hidden`，需要满屏高度时用 `.h-screen-safe` 工具类（**不要裸写 `100dvh`**，老内核会塌缩），不依赖图片资源。

---

### 10.9 错误 / 降级页

#### 设计要点

**按错误类型区分标题，不用通用「出错了」。**

| 类型 | 标题 | 操作 |
|------|------|------|
| AI 服务不可用 | 「AI 服务暂时没响应」 | 主「重试」+ 次「留个邮箱，好了推给你」 |
| 网络中断 | 「网络好像断了」 | 主「重试」 |
| 404 | 「这个页面不存在」 | 主「回首页」 |
| 限流 | 「请求有点多，稍等一下」 | 主「重试」（带倒计时 `.num`） |

- `circle-alert` 32px `--danger`
- 技术错误码收进折叠的「技术详情」（`--meta` + `--font-mono`），默认收起——给排查用，不吓用户
- **绝不白屏，绝不向用户暴露原始技术错误**

#### 前端还原提示词

> 错误页/错误块通用组件。居中布局，最大宽 480px，容器加 `.h-screen-safe` 后减去顶栏高度垂直居中（**不要裸写 `100dvh`**）。
>
> 结构：`circle-alert` 32px `--danger`（`--danger-wash` 圆底 56×56 承托）→ 标题（`--text-xl`/600，**按 errorType 取真实文案，不用通用「出错了」**）→ 说明（`--text-base` `--muted`）→ 按钮组（Primary + 可选 Secondary，各 44px）→ 折叠「技术详情」（`<details>`，`--text-xs` `--font-mono` `--meta`，默认 collapsed）。
>
> 支持 errorType：`ai_unavailable` / `network` / `not_found` / `rate_limited`（带 `.num` 倒计时禁用重试按钮）。
>
> 同一组件需支持「整页」与「模块内嵌降级」两种尺寸（内嵌时图标降为 24px、去掉垂直居中）。

---

### 10.10 隐私政策 / 用户协议

#### 设计要点

**两份独立文档，不塞进页脚小字**（PIPL 合规要求 + PM 明确指示）。

- `--container-reading` 720px
- `--text-base` + `--leading-relaxed` 1.75，段落间距 `--space-5`
- 桌面 sticky 侧栏目录锚点；移动端顶部折叠目录
- 顶部「最近更新：2026-08-03」（`.num` `--meta`）
- 标题层级清晰，h2 前留 `--space-8`

#### 前端还原提示词

> 长文档页。容器 `--container-reading` 720px，正文 `--text-base` + `--leading-relaxed`，段落 `margin-bottom: var(--space-5)`，h2 `margin-top: var(--space-8)`。
>
> 桌面 ≥1024 左侧加 200px sticky 目录（`top: 88px`），当前章节高亮 `--accent`；移动端改为顶部可折叠目录（`<details>`）。
>
> 顶部「最近更新」`.num` `--meta`。列表项 `--fg-2`，重点条款可用 `--surface-sunken` 引用块承托。**不缩小字号，不降低对比度。**

---

## 11. 组件状态矩阵

所有有状态组件必须覆盖以下 9 态（Success 视场景可选）：

| 组件 | Default | Hover | Focus | Active | Disabled | Loading | Error | Empty | Success |
|------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| Button | 必须 | 必须 | 必须 | 必须 | 必须 | 必须 | — | — | 可选 |
| Input / Textarea | 必须 | 必须 | 必须 | — | 必须 | — | 必须 | — | 可选 |
| Chip / Segmented | 必须 | 必须 | 必须 | 必须 | 必须 | — | — | — | — |
| Checkbox（隐私） | 必须 | 必须 | 必须 | 必须 | — | — | 必须 | — | — |
| 文件上传 | 必须 | 必须 | 必须 | — | — | 必须 | 必须 | 必须 | 必须 |
| 匹配度卡 | 必须 | — | 必须 | — | — | 必须 | 必须 | — | — |
| 能力列表 | 必须 | — | — | — | — | 必须 | 必须 | 必须 | — |
| 学习路径阶段 | 必须 | — | 必须 | — | — | 必须 | 必须 | — | — |
| sticky CTA | 必须 | 必须 | 必须 | 必须 | — | 必须 | — | — | — |
| 弹层 | 必须 | — | 必须 | — | — | 必须 | 必须 | — | 必须 |

### 五态内容规范（异步模块）

| 状态 | 规范 |
|------|------|
| Loading | 骨架屏必须**按真实结构分块**，不用通用灰条。禁止假进度百分比 |
| Empty | 必须有引导文案 + 一个明确操作按钮，`inbox` 24px `--meta`。禁止只显示"暂无数据" |
| Error | 必须有**具体**错误说明 + 重试按钮。技术码折叠。禁止暴露堆栈 |
| Populated | 正常渲染 |
| Edge | 超长文本不截断（允许容器增高）；零结果不硬凑（诚实说明覆盖范围）；置信度低时显式标注 |

---

## 12. 无障碍基线（WCAG 2.1 AA）

| 项 | 要求 | 本系统落地 |
|----|------|-----------|
| 文本对比度 | ≥ 4.5:1 | 四级前景全部达标（17.09 / 10.40 / 5.54 / 4.67），语义色 4.98–6.13:1 |
| 图形对比度 | ≥ 3:1 | `--data-3` 3.75:1，刻度条与槽有明确边界 |
| 触摸目标 | ≥ 44×44px | `--tap-min` 全局兜底 |
| 键盘可达 | 全部交互 | `:focus-visible` 全局 `--focus-ring`，严禁裸 `outline:none` |
| 非颜色编码 | 不单靠颜色 | 匹配度三重编码；chip 选中带 `check` 图标；状态带图标 + 文字 |
| 图标按钮 | 必须有名 | 所有纯图标按钮强制 `aria-label` |
| 动效 | 可关闭 | `prefers-reduced-motion` 全局降级 |
| 缩放 | 不禁用 | 严禁 `user-scalable=no` |
| 弹层 | 焦点管理 | `role="dialog"` + focus trap + Esc + 焦点归还 |

---

## 13. Agent Prompt Guide（给前端 Agent 的实现要点）

1. **引入方式**：`import tokens from './design-tokens.json'` 或直接 `@import './tokens.css'`。组件内**一律** `var(--token)`，唯一例外 `#fff` / `#000`。任何硬编码色值视为缺陷。
2. **图标**：只用 Lucide，且必须经架构师 Spec 第 8.1 章的 `src/ui/Icon.tsx` 统一出口引入，业务组件不直接 `import lucide-react`。线宽由档位决定：16px→`2`、20px→`1.75`、24px→`1.5`（三档实渲染 1.33/1.46/1.50px，视觉重量恒定），颜色 `currentColor`。按语义映射表取名，不要自己换。**任何位置都不许用 emoji。**
3. **数字**：匹配度、年限、阶段时长、报告编号、倒计时一律加 `.num` 类。三个赛道的匹配度数字必须纵向对齐。
4. **列表 vs 卡片**：能力列表用 `divide-y`；只有赛道匹配（3 个并列决策项）用卡片，且**三张必须不等权重**。
5. **强调色预算**：每屏可见的饱和 `--accent` 使用 ≤ 2 处。需要"看起来重要"时优先用 `--accent-wash` + `--accent-border`，把饱和色留给唯一的主行动。
6. **sticky CTA**：`body` 必须留 `padding-bottom`；滚动 20% 后才淡入；页内完整 CTA 区进入视口时淡出。这三条缺一不可，否则就变成了"遮挡结果"。
7. **不留死胡同**：任何失败态都必须给下一步动作。简历解析失败 → 一键切表单；AI 失败 → 重试或留邮箱；链接过期 → 引导自己做诊断。
8. **文案纪律**：禁止绝对化表述（"保证入职""薪资翻倍""100% 匹配"）；禁止虚构指标；所有示例必须是真实工程场景表述。
9. **移动优先**：所有 media query 用 `min-width`。先把 375px 做对，再加宽。满屏高度一律用 `.h-screen-safe` / `.h-screen-fixed`，**严禁裸写 `100dvh`**（`dvh` 需 Chrome 108+，低版本内核会整条丢弃导致高度塌缩）。同理不要使用 `color-mix()`、`:has()`、`@container`、`oklch()` —— 本项目兼容下限为 Chrome 88 级内核（见 ADR-002）。
10. **五态齐全才算完成**：Loading / Empty / Error / Populated / Edge 缺一不可，尤其 Error 与 Edge——目标用户对"崩了"的容忍度极低，一次白屏就再也不来了。

---

## 14. 自检结论

| P0 规则 | 结论 |
|---------|------|
| 无 emoji 作功能图标 | 通过。全部产出文件经正则 `[\x{1F300}-\x{1F9FF}\x{2600}-\x{26FF}\x{2700}-\x{27BF}]` 扫描，零命中。图标统一 Lucide |
| 无紫→粉渐变 | 通过。**全站零渐变**。主色 `#14507D` 蓝晒靛，层级靠表面明度递进 |
| 无 AI 模板味 | 通过。首屏为真实翻译对照卡 + 可填表单，非「大标题+居中 CTA+抽象 3D」；无 "Welcome to" / Lorem ipsum；所有示例为真实工程表述 |
| 无硬编码颜色 | 通过。全部 Token 化，四层架构（A1/A2/B-slot/C-extension） |
| 图标库已锁定 | 通过。Lucide（`lucide-react` 1.26.0），16/20/24px，stroke 2/1.75/1.5，与架构师 Spec §8.1 及 ADR-008 一致 |

| 质量项 | 结论 |
|--------|------|
| 对比度 | 前景四级 17.09 / 10.40 / 5.54 / 4.67，语义色 4.98–6.13:1，图表色 ≥3:1，全部达 AA |
| 间距 | 全部 4px 整数倍 |
| 圆角 | 上限 14px，无过度圆角 |
| 阴影 | 无幽灵卡片（ring 与 raised 互斥） |
| 动效 | ≤300ms，`prefers-reduced-motion` 全局降级 |
| 触摸 | ≥44×44px 全局兜底 |
| 状态覆盖 | 9 态矩阵 + 五态内容规范齐全 |
| 响应式 | 移动优先，含微信内置浏览器 7 项专项处理 |

---

*文档结束。配套 `design-tokens.json`、`tokens.css` 可直接交付前端。*
